# Build directories (build/, build-werror/, build-release/, ...)
build*/

# Runtime save file — see SAVE_FILE_PATH in src/game.h
saltmarch_save.dat

# Recorded command-log / replay files and test scratch
*.smlog
test_*.tmp

# Shared-feed runtime files (Phase 4) — per-machine, never committed
feed_out.jsonl
feed_in.jsonl
